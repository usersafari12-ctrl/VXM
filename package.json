{
  "name": "vbm-proxy",
  "version": "2.0.0",
  "description": "Voxiom Bot Manager — auth + payload server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
